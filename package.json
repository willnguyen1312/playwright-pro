{
  "name": "playwright-pro",
  "private": true,
  "version": "0.0.0",
  "type": "module",
  "author": {
    "name": "Nam Nguyen",
    "email": "nam.nguyen.node@gmail.com"
  },
  "scripts": {
    "dev": "vite",
    "build": "tsc && vite build",
    "preview": "vite preview",
    "test": "pnpm exec playwright test",
    "test:ui": "pnpm exec playwright test --ui",
    "test:trace": "pnpm exec playwright test --trace on",
    "test:report": "pnpm exec playwright show-report",
    "test:debug": "pnpm exec playwright test --debug",
    "test:generate": "pnpm exec playwright codegen demo.playwright.dev/todomvc"
  },
  "devDependencies": {
    "@playwright/test": "^1.51.1",
    "@types/node": "^22.13.14",
    "typescript": "~5.7.2",
    "vite": "^6.2.0"
  }
}
